# api-sammyhost.uk
API for sammyhost.uk

> [!WARNING]
> Make sure to install dependencies first.
> ```sh
> pip install -r requirements.txt
> ```

> [!IMPORTANT]
> To start, use the following command in terminal.
> ```
> uvicorn main:app --reload --header server:sammyhost.uk
> ```
